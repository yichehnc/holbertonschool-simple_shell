/**
 * main - entry point to the program
 * @argc: argument count
 * @argv: array of string literals passed as argument to main
 * Return: 0 if no error, else a value other than zero
 */
int main(int argc, char const *argv[])
{
	printf("Hello shell\n");
	return (0);
}
